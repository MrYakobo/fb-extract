Fix linking to photos/videos? How would one do that?
Fix the flickering from updating the terminal progress. It's horrible
Fix emoji support? Trivial or impossible [x] It is already fixed. Use something else than command line programs to view the emojis.
